<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ml.mldj.system.mapper.SysRoleMapper">

    <select id="queryRoleByUserId" resultType="org.ml.mldj.model.system.entity.SysRole">
        select r.*
        from sys_user_role ur
                 inner join sys_role r on ur.user_id = #{userId} and ur.role_id = r.id
        where (r.is_deleted = 0 or r.is_deleted is null)
    </select>
</mapper>
